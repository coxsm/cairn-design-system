Plain bold uppercase heading — titles every block on the Rules Summary side.

```jsx
<SectionHeading>Saves</SectionHeading>
<SectionHeading size="lg">Inventory</SectionHeading>
```

Bare bold caps, no underline (the printout's headers carry no rule). Two sizes only.
